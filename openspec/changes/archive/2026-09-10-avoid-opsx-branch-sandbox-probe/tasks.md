## 1. Managed Protocol Contract

- [x] 1.1 Add a focused portable-protocol assertion for first-attempt Git-write permission handling and verify it fails before the template change
- [x] 1.2 Update the managed protocol template, run the installer, and verify the focused test passes with template and installed output synchronized

## 2. Validation

- [x] 2.1 Run the affected workflow/protocol tests, strict OpenSpec validation, and diff checks
